# Client

Install dependencies firstly:

```sh
$: yarn install
```

Dev bundles:

```sh
$: yarn build
```

Dev bundles in watch mode:

```sh
$: yarn build:watch
```

Prod bundles:

```sh
$: yarn build:prod
```

Testing:

```sh
$: yarn test
```
